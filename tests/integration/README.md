# integration tests

Boot-level tests wiring our modules into real host services (storage domain, agents, presets)
outside the browser. Today these run as scripted probe rounds (spikes/probe/lib/roundNN.js
against scratch profiles); promotion into this directory is tracked in docs/development.md.

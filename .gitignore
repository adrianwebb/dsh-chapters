/examples/
/node_modules/
/lib/
/.npmcache/
/spikes/probe/node_modules/
/test-results/

spikes/probe/results-*.json
spikes/probe/created-ids.json

.dshdev*/
.pnpm-store/
/.dsh-chapters/
/var/  # pure scratch again — the model tapes now live (tracked) in tests/fixtures/model-tape/
/.dsh-chapters*/
.dsh-knowledge/
.dsh-chapters*/
/playwright-report/
/.dsh/
/var/
/tests/e2e/var/

# generated probe outputs (regenerated by a run; keep the round* source scripts)
spikes/probe/results.json
spikes/probe/round*-state.json

# e2e self-heal: mirror residue moved aside (docs: ensureClone dirty-dir policy)
.dsh-knowledge.stale-*/
.dsh-chapters.stale-*/

# c8 raw V8 profile scratch (report dir is var/coverage; this is temp)
coverage/
